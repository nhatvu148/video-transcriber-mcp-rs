# Rust
/target/
**/*.rs.bk
*.pdb

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Models (large files)
models/
*.bin

# Temporary files
*.tmp
*.log

# Output
transcripts/

# Local credits ledger (engine writes here when CREDITS_DB_PATH is unset —
# happens during `cargo run` for dev. In prod, Fly sets the path to a volume).
credits.json

# Secrets — never commit
.env
.env.local
.env.*.local

# Task tool's internal cache (regenerated on every build)
.task/
